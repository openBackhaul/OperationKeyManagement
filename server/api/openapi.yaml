openapi: 3.0.0
info:
  title: OperationKeyManagement
  version: 2.0.0
servers:
- url: /
paths:
  /v1/bequeath-your-data-and-die:
    post:
      tags:
      - IndividualServices
      summary: Initiates process of embedding a new release
      operationId: bequeathYourDataAndDie
      parameters:
      - name: user
        in: header
        description: User identifier from the system starting the service call
        required: true
        style: simple
        explode: false
        schema:
          type: string
          example: User Name
      - name: originator
        in: header
        description: |
          'Identification for the system consuming the API, as defined in
           [/core-model-1-4:control-construct/logical-termination-point={uuid}/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
        required: true
        style: simple
        explode: false
        schema:
          minLength: 3
          type: string
          example: Resolver
      - name: x-correlator
        in: header
        description: UUID for the service execution flow that allows to correlate
          requests and responses
        required: true
        style: simple
        explode: false
        schema:
          pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
          type: string
          description: Empty string accepted from external applications.
          example: 550e8400-e29b-11d4-a716-446655440000
      - name: trace-indicator
        in: header
        description: Sequence of request numbers along the flow
        required: true
        style: simple
        explode: false
        schema:
          pattern: "^([0-9]+)(\\.([0-9]+))*$"
          type: string
          description: Empty string accepted from external applications.
          example: 1.3.1
      - name: customer-journey
        in: header
        description: Holds information supporting customer’s journey to which the
          execution applies
        required: true
        style: simple
        explode: false
        schema:
          type: string
          example: Unknown value
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/v1_bequeathyourdataanddie_body'
        required: true
      responses:
        "204":
          description: Process of embedding a new release initiated
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
            life-cycle-state:
              description: |
                'Life cycle state of the consumed service find in [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-op-s-im-000/layer-protocol=0/operation-server-interface-1-0:operation-server-interface-pac/operation-server-interface-configuration/life-cycle-state]'
              style: simple
              explode: false
              schema:
                type: string
                example: EXPERIMENTAL
                enum:
                - EXPERIMENTAL
                - OPERATIONAL
                - DEPRECATED
                - OBSOLETE
                - UNKNOWN
                - NOT_YET_DEFINED
        "400":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        "401":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        "403":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        "404":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        "500":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        default:
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
      callbacks:
        PromptForBequeathingDataCausesTransferOfListOfApplications:
          url:
            post:
              parameters:
              - name: user
                in: header
                description: User identifier from the system starting the service
                  call
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: User Name
              - name: originator
                in: header
                description: |
                  'Identification for the system consuming the API, as defined in
                   [/core-model-1-4:control-construct/logical-termination-point={uuid}/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
                required: true
                style: simple
                explode: false
                schema:
                  minLength: 3
                  type: string
                  example: Resolver
              - name: x-correlator
                in: header
                description: UUID for the service execution flow that allows to correlate
                  requests and responses
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 550e8400-e29b-11d4-a716-446655440000
              - name: trace-indicator
                in: header
                description: Sequence of request numbers along the flow
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^([0-9]+)(\\.([0-9]+))*$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 1.3.1
              - name: customer-journey
                in: header
                description: Holds information supporting customer’s journey to which
                  the execution applies
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: Unknown value
              requestBody:
                content:
                  application/json:
                    schema:
                      required:
                      - address
                      - application-name
                      - port
                      - protocol
                      - release-number
                      type: object
                      properties:
                        application-name:
                          type: string
                          description: |
                            'Name of application that shall be part of the operationKey management from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-c-*/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
                        release-number:
                          type: string
                          description: |
                            'Release of application that shall be part of the operationKey management from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-c-*/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/release-number]'
                        protocol:
                          type: string
                          description: |
                            'Protocol to be used for operationKey management from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-*/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-protocol]'
                        address:
                          type: object
                          properties:
                            ip-address:
                              type: object
                              properties:
                                ipv-4-address:
                                  type: string
                                  description: |
                                    'IP address of application that shall be part of the operationKey management from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-*/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-address/ip-address/ipv-4-address]'
                            domain-name:
                              type: string
                              description: |
                                'Domain name of application that shall be part of the operationKey management from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-*/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-address/domain-name]'
                        port:
                          type: integer
                          description: |
                            'TCP port of application that shall be part of the operationKey management from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-*/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-port]'
                      example:
                        application-name: RegistryOffice
                        release-number: 2.0.0
                        protocol: HTTP
                        address:
                          ip-address:
                            ipv-4-address: 1.1.3.8
                        port: 3008
                required: true
              responses:
                "204":
                  description: Application will be able to receive operationKeys
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                    life-cycle-state:
                      description: Life cycle state of the consumed service
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: EXPERIMENTAL
                        enum:
                        - EXPERIMENTAL
                        - OPERATIONAL
                        - DEPRECATED
                        - OBSOLETE
                        - UNKNOWN
                        - NOT_YET_DEFINED
                "400":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "401":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "403":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "404":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "500":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                default:
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
        PromptForBequeathingDataCausesRObeingRequestedToNotifyApprovalsOfNewApplicationsToNewRelease:
          url:
            post:
              parameters:
              - name: user
                in: header
                description: User identifier from the system starting the service
                  call
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: User Name
              - name: originator
                in: header
                description: |
                  'Identification for the system consuming the API, as defined in
                   [/core-model-1-4:control-construct/logical-termination-point={uuid}/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
                required: true
                style: simple
                explode: false
                schema:
                  minLength: 3
                  type: string
                  example: Resolver
              - name: x-correlator
                in: header
                description: UUID for the service execution flow that allows to correlate
                  requests and responses
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 550e8400-e29b-11d4-a716-446655440000
              - name: trace-indicator
                in: header
                description: Sequence of request numbers along the flow
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^([0-9]+)(\\.([0-9]+))*$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 1.3.1
              - name: customer-journey
                in: header
                description: Holds information supporting customer’s journey to which
                  the execution applies
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: Unknown value
              requestBody:
                content:
                  application/json:
                    schema:
                      required:
                      - subscriber-address
                      - subscriber-application
                      - subscriber-operation
                      - subscriber-port
                      - subscriber-protocol
                      - subscriber-release-number
                      type: object
                      properties:
                        subscriber-application:
                          type: string
                          description: |
                            'Future application name from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-c-nr-2-0-0-000/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
                        subscriber-release-number:
                          type: string
                          description: |
                            'Future release number from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-c-nr-2-0-0-000/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/release-number]'
                        subscriber-operation:
                          type: string
                          description: |
                            'Names of the operation that shall be addressed by notifications about new approvals from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-op-s-is-001/layer-protocol=0/operation-server-interface-1-0:operation-server-interface-pac/operation-server-interface-capability/operation-name]'
                        subscriber-protocol:
                          type: string
                          description: |
                            'Protocol to be used for addressing the application that wants to receive notifications about new approvals from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-nr-2-0-0-000/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-protocol]'
                        subscriber-address:
                          type: object
                          properties:
                            ip-address:
                              type: object
                              properties:
                                ipv-4-address:
                                  type: string
                                  description: |
                                    'IPv4 address of application that wants to receive notifications about new approvals from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-nr-2-0-0-000/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-address/ip-address/ipv-4-address]'
                            domain-name:
                              type: string
                              description: |
                                'Domain name of application that wants to receive notifications about new approvals from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-nr-2-0-0-000/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-address/domain-name]'
                        subscriber-port:
                          type: integer
                          description: |
                            'TCP port of application that wants to receive notifications about new approvals from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-nr-2-0-0-000/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-port]'
                      example:
                        subscriber-application: OperationKeyManagement
                        subscriber-release-number: 2.0.0
                        subscriber-operation: /v1/regard-application
                        subscriber-protocol: HTTP
                        subscriber-address:
                          ip-address:
                            ipv-4-address: 1.1.3.14
                        subscriber-port: 3014
                required: true
              responses:
                "204":
                  description: Subscribed for notifications about new approvals
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                    life-cycle-state:
                      description: Life cycle state of the consumed service
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: EXPERIMENTAL
                        enum:
                        - EXPERIMENTAL
                        - OPERATIONAL
                        - DEPRECATED
                        - OBSOLETE
                        - UNKNOWN
                        - NOT_YET_DEFINED
                "400":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "401":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "403":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "404":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "500":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                default:
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
        PromptForBequeathingDataCausesRObeingRequestedToNotifyWithdrawnApprovalsToNewRelease:
          url:
            post:
              parameters:
              - name: user
                in: header
                description: User identifier from the system starting the service
                  call
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: User Name
              - name: originator
                in: header
                description: |
                  'Identification for the system consuming the API, as defined in
                   [/core-model-1-4:control-construct/logical-termination-point={uuid}/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
                required: true
                style: simple
                explode: false
                schema:
                  minLength: 3
                  type: string
                  example: Resolver
              - name: x-correlator
                in: header
                description: UUID for the service execution flow that allows to correlate
                  requests and responses
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 550e8400-e29b-11d4-a716-446655440000
              - name: trace-indicator
                in: header
                description: Sequence of request numbers along the flow
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^([0-9]+)(\\.([0-9]+))*$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 1.3.1
              - name: customer-journey
                in: header
                description: Holds information supporting customer’s journey to which
                  the execution applies
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: Unknown value
              requestBody:
                content:
                  application/json:
                    schema:
                      required:
                      - subscriber-address
                      - subscriber-application
                      - subscriber-operation
                      - subscriber-port
                      - subscriber-protocol
                      - subscriber-release-number
                      type: object
                      properties:
                        subscriber-application:
                          type: string
                          description: |
                            'Future application name from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-c-nr-2-0-0-000/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
                        subscriber-release-number:
                          type: string
                          description: |
                            'Future release number from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-c-nr-2-0-0-000/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/release-number]'
                        subscriber-operation:
                          type: string
                          description: |
                            'Names of the operation that shall be addressed by notifications about withdrawn approvals from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-op-s-is-002/layer-protocol=0/operation-server-interface-1-0:operation-server-interface-pac/operation-server-interface-capability/operation-name]'
                        subscriber-protocol:
                          type: string
                          description: |
                            'Protocol to be used for addressing the application that wants to receive notifications about withdrawn approvals from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-nr-2-0-0-000/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-protocol]'
                        subscriber-address:
                          type: object
                          properties:
                            ip-address:
                              type: object
                              properties:
                                ipv-4-address:
                                  type: string
                                  description: |
                                    'IPv4 address of application that wants to receive notifications about withdrawn approvals from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-nr-2-0-0-000/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-address/ip-address/ipv-4-address]'
                            domain-name:
                              type: string
                              description: |
                                'Domain name of application that wants to receive notifications about withdrawn approvals from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-nr-2-0-0-000/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-address/domain-name]'
                        subscriber-port:
                          type: integer
                          description: |
                            'TCP port of application that wants to receive notifications about withdrawn approvals from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-nr-2-0-0-000/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-port]'
                      example:
                        subscriber-application: OperationKeyManagement
                        subscriber-release-number: 2.0.0
                        subscriber-operation: /v1/disregard-application
                        subscriber-protocol: HTTP
                        subscriber-address:
                          ip-address:
                            ipv-4-address: 1.1.3.14
                        subscriber-port: 3014
                required: true
              responses:
                "204":
                  description: Subscribed for notifications about withdrawn approvals
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                    life-cycle-state:
                      description: Life cycle state of the consumed service
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: EXPERIMENTAL
                        enum:
                        - EXPERIMENTAL
                        - OPERATIONAL
                        - DEPRECATED
                        - OBSOLETE
                        - UNKNOWN
                        - NOT_YET_DEFINED
                "400":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "401":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "403":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "404":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "500":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                default:
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
        PromptForBequeathingDataCausesALTbeingRequestedToNotifyLinkUpdatesToNewRelease:
          url:
            post:
              parameters:
              - name: user
                in: header
                description: User identifier from the system starting the service
                  call
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: User Name
              - name: originator
                in: header
                description: |
                  'Identification for the system consuming the API, as defined in
                   [/core-model-1-4:control-construct/logical-termination-point={uuid}/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
                required: true
                style: simple
                explode: false
                schema:
                  minLength: 3
                  type: string
                  example: Resolver
              - name: x-correlator
                in: header
                description: UUID for the service execution flow that allows to correlate
                  requests and responses
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 550e8400-e29b-11d4-a716-446655440000
              - name: trace-indicator
                in: header
                description: Sequence of request numbers along the flow
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^([0-9]+)(\\.([0-9]+))*$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 1.3.1
              - name: customer-journey
                in: header
                description: Holds information supporting customer’s journey to which
                  the execution applies
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: Unknown value
              requestBody:
                content:
                  application/json:
                    schema:
                      required:
                      - subscriber-address
                      - subscriber-application
                      - subscriber-operation
                      - subscriber-port
                      - subscriber-protocol
                      - subscriber-release-number
                      type: object
                      properties:
                        subscriber-application:
                          type: string
                          description: |
                            'Future application name from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-c-nr-2-0-0-000/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
                        subscriber-release-number:
                          type: string
                          description: |
                            'Future release number from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-c-nr-2-0-0-000/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/release-number]'
                        subscriber-operation:
                          type: string
                          description: |
                            'Name of the operation that shall be addressed by notifications about updates of Links from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-op-s-is-004/layer-protocol=0/operation-server-interface-1-0:operation-server-interface-pac/operation-server-interface-capability/operation-name]'
                        subscriber-protocol:
                          type: string
                          description: |
                            'Protocol to be used for addressing the application that wants to receive notifications about updates of Links from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-nr-2-0-0-000/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-protocol]'
                        subscriber-address:
                          type: object
                          properties:
                            ip-address:
                              type: object
                              properties:
                                ipv-4-address:
                                  type: string
                                  description: |
                                    'IPv4 address of application that wants to receive notifications about updates of Links from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-nr-2-0-0-000/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-address/ip-address/ipv-4-address]'
                            domain-name:
                              type: string
                              description: |
                                'Domain name of application that wants to receive notifications about updates of Links from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-nr-2-0-0-000/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-address/domain-name]'
                        subscriber-port:
                          type: integer
                          description: |
                            'TCP port of application that wants to receive notifications about updates of Links from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-nr-2-0-0-000/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-port]'
                      example:
                        subscriber-application: OperationKeyManagement
                        subscriber-release-number: 2.0.0
                        subscriber-operation: /v1/regard-updated-link
                        subscriber-protocol: HTTP
                        subscriber-address:
                          ip-address:
                            ipv-4-address: 1.1.3.14
                        subscriber-port: 3014
                required: true
              responses:
                "204":
                  description: Subscribed for notifications about updates of Links
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                    life-cycle-state:
                      description: Life cycle state of the consumed service
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: EXPERIMENTAL
                        enum:
                        - EXPERIMENTAL
                        - OPERATIONAL
                        - DEPRECATED
                        - OBSOLETE
                        - UNKNOWN
                        - NOT_YET_DEFINED
                "400":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "401":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "403":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "404":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "500":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                default:
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
        PromptForBequeathingDataCausesRObeingRequestedToStopNotificationsToOldRelease:
          url:
            post:
              parameters:
              - name: user
                in: header
                description: User identifier from the system starting the service
                  call
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: User Name
              - name: originator
                in: header
                description: |
                  'Identification for the system consuming the API, as defined in
                   [/core-model-1-4:control-construct/logical-termination-point={uuid}/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
                required: true
                style: simple
                explode: false
                schema:
                  minLength: 3
                  type: string
                  example: Resolver
              - name: x-correlator
                in: header
                description: UUID for the service execution flow that allows to correlate
                  requests and responses
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 550e8400-e29b-11d4-a716-446655440000
              - name: trace-indicator
                in: header
                description: Sequence of request numbers along the flow
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^([0-9]+)(\\.([0-9]+))*$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 1.3.1
              - name: customer-journey
                in: header
                description: Holds information supporting customer’s journey to which
                  the execution applies
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: Unknown value
              requestBody:
                content:
                  application/json:
                    schema:
                      required:
                      - subscriber-application
                      - subscriber-release-number
                      - subscription
                      type: object
                      properties:
                        subscriber-application:
                          type: string
                          description: |
                            'Own application name from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-s-000/layer-protocol=0/http-server-interface-1-0:http-server-interface-pac/http-server-interface-capability/application-name]'
                        subscriber-release-number:
                          type: string
                          description: |
                            'Own release number from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-s-000/layer-protocol=0/http-server-interface-1-0:http-server-interface-pac/http-server-interface-capability/release-number]'
                        subscription:
                          type: string
                          description: |
                            'Name of operation that had been addressed for starting getting notifications from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-op-c-im-ro-2-0-0-000/layer-protocol=0/operation-client-interface-1-0:operation-client-interface-pac/operation-client-interface-configuration/operation-name] and from  [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-op-c-im-ro-2-0-0-001/layer-protocol=0/operation-client-interface-1-0:operation-client-interface-pac/operation-client-interface-configuration/operation-name] in two separate, sequential requests.'
                      example:
                        subscriber-application: OperationKeyManagement
                        subscriber-release-number: 1.0.0
                        subscription: /v1/notify-approvals
                required: true
              responses:
                "204":
                  description: Stopped sending notifications of a specific subscription
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                    life-cycle-state:
                      description: Life cycle state of the consumed service
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: EXPERIMENTAL
                        enum:
                        - EXPERIMENTAL
                        - OPERATIONAL
                        - DEPRECATED
                        - OBSOLETE
                        - UNKNOWN
                        - NOT_YET_DEFINED
                "400":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "401":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "403":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "404":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "500":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                default:
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
        PromptForBequeathingDataCausesALTbeingRequestedToStopNotificationsToOldRelease:
          url:
            post:
              parameters:
              - name: user
                in: header
                description: User identifier from the system starting the service
                  call
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: User Name
              - name: originator
                in: header
                description: |
                  'Identification for the system consuming the API, as defined in
                   [/core-model-1-4:control-construct/logical-termination-point={uuid}/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
                required: true
                style: simple
                explode: false
                schema:
                  minLength: 3
                  type: string
                  example: Resolver
              - name: x-correlator
                in: header
                description: UUID for the service execution flow that allows to correlate
                  requests and responses
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 550e8400-e29b-11d4-a716-446655440000
              - name: trace-indicator
                in: header
                description: Sequence of request numbers along the flow
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^([0-9]+)(\\.([0-9]+))*$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 1.3.1
              - name: customer-journey
                in: header
                description: Holds information supporting customer’s journey to which
                  the execution applies
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: Unknown value
              requestBody:
                content:
                  application/json:
                    schema:
                      required:
                      - subscriber-application
                      - subscriber-release-number
                      - subscription
                      type: object
                      properties:
                        subscriber-application:
                          type: string
                          description: |
                            'Own application name from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-s-000/layer-protocol=0/http-server-interface-1-0:http-server-interface-pac/http-server-interface-capability/application-name]'
                        subscriber-release-number:
                          type: string
                          description: |
                            'Own release number from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-s-000/layer-protocol=0/http-server-interface-1-0:http-server-interface-pac/http-server-interface-capability/release-number]'
                        subscription:
                          type: string
                          description: |
                            'Name of operation that had been addressed for starting getting notifications from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-op-c-im-alt-1-0-0-000/layer-protocol=0/operation-client-interface-1-0:operation-client-interface-pac/operation-client-interface-configuration/operation-name]'
                      example:
                        subscriber-application: OperationKeyManagement
                        subscriber-release-number: 2.0.0
                        subscription: /v1/notify-link-updates
                required: true
              responses:
                "204":
                  description: Stopped sending notifications of a specific subscription
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                    life-cycle-state:
                      description: Life cycle state of the consumed service
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: EXPERIMENTAL
                        enum:
                        - EXPERIMENTAL
                        - OPERATIONAL
                        - DEPRECATED
                        - OBSOLETE
                        - UNKNOWN
                        - NOT_YET_DEFINED
                "400":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "401":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "403":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "404":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "500":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                default:
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
        PromptForBequeathingDataCausesRequestForBroadcastingInfoAboutServerReplacement:
          url:
            post:
              parameters:
              - name: user
                in: header
                description: User identifier from the system starting the service
                  call
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: User Name
              - name: originator
                in: header
                description: |
                  'Identification for the system consuming the API, as defined in
                   [/core-model-1-4:control-construct/logical-termination-point={uuid}/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
                required: true
                style: simple
                explode: false
                schema:
                  minLength: 3
                  type: string
                  example: Resolver
              - name: x-correlator
                in: header
                description: UUID for the service execution flow that allows to correlate
                  requests and responses
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 550e8400-e29b-11d4-a716-446655440000
              - name: trace-indicator
                in: header
                description: Sequence of request numbers along the flow
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^([0-9]+)(\\.([0-9]+))*$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 1.3.1
              - name: customer-journey
                in: header
                description: Holds information supporting customer’s journey to which
                  the execution applies
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: Unknown value
              requestBody:
                content:
                  application/json:
                    schema:
                      required:
                      - current-application-name
                      - current-release-number
                      - future-address
                      - future-application-name
                      - future-port
                      - future-protocol
                      - future-release-number
                      type: object
                      properties:
                        current-application-name:
                          type: string
                          description: |
                            'Own application name from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-s-000/layer-protocol=0/http-server-interface-1-0:http-server-interface-pac/http-server-interface-capability/application-name]'
                        current-release-number:
                          type: string
                          description: |
                            'Own release number from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-s-000/layer-protocol=0/http-server-interface-1-0:http-server-interface-pac/http-server-interface-capability/release-number]'
                        future-application-name:
                          type: string
                          description: |
                            'Future name of the application that has updated connection data from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-c-nr-2-0-0-000/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
                        future-release-number:
                          type: string
                          description: |
                            'Future release number of the application that has updated connection data from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-c-nr-2-0-0-000/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/release-number]'
                        future-protocol:
                          type: string
                          description: |
                            'Protocol to be used in future for addressing the application that has updated connection data from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-nr-2-0-0-000/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-protocol]'
                        future-address:
                          type: object
                          properties:
                            ip-address:
                              type: object
                              properties:
                                ipv-4-address:
                                  type: string
                                  description: |
                                    'Future IPv4 address of the application that has updated connection data from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-nr-2-0-0-000/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-address/ip-address/ipv-4-address]'
                            domain-name:
                              type: string
                              description: |
                                'Future domain name of the application that has updated connection data from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-nr-2-0-0-000/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-address/domain-name]'
                        future-port:
                          type: integer
                          description: |
                            'Future port of the application that has updated connection data from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-nr-2-0-0-000/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-port]'
                      example:
                        current-application-name: OperationKeyManagement
                        current-release-number: 1.0.0
                        future-application-name: OperationKeyManagement
                        future-release-number: 2.0.0
                        future-protocol: HTTP
                        future-address:
                          ip-address:
                            ipv-4-address: 1.1.3.13
                required: true
              responses:
                "204":
                  description: Will broadcast server replacement
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                    life-cycle-state:
                      description: Life cycle state of the consumed service
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: EXPERIMENTAL
                        enum:
                        - EXPERIMENTAL
                        - OPERATIONAL
                        - DEPRECATED
                        - OBSOLETE
                        - UNKNOWN
                        - NOT_YET_DEFINED
                "400":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "401":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "403":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "404":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "500":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                default:
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
        PromptForBequeathingDataCausesRequestForDeregisteringOfOldRelease:
          url:
            post:
              parameters:
              - name: user
                in: header
                description: User identifier from the system starting the service
                  call
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: User Name
              - name: originator
                in: header
                description: |
                  'Identification for the system consuming the API, as defined in
                   [/core-model-1-4:control-construct/logical-termination-point={uuid}/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
                required: true
                style: simple
                explode: false
                schema:
                  minLength: 3
                  type: string
                  example: Resolver
              - name: x-correlator
                in: header
                description: UUID for the service execution flow that allows to correlate
                  requests and responses
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 550e8400-e29b-11d4-a716-446655440000
              - name: trace-indicator
                in: header
                description: Sequence of request numbers along the flow
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^([0-9]+)(\\.([0-9]+))*$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 1.3.1
              - name: customer-journey
                in: header
                description: Holds information supporting customer’s journey to which
                  the execution applies
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: Unknown value
              requestBody:
                content:
                  application/json:
                    schema:
                      required:
                      - application-name
                      - release-number
                      type: object
                      properties:
                        application-name:
                          type: string
                          description: |
                            'Own application name from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-s-000/layer-protocol=0/http-server-interface-1-0:http-server-interface-pac/http-server-interface-capability/application-name]'
                        release-number:
                          type: string
                          description: |
                            'Own release number from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-s-000/layer-protocol=0/http-server-interface-1-0:http-server-interface-pac/http-server-interface-capability/release-number]'
                      example:
                        application-name: BadApplication
                        release-number: 1.0.0
                required: true
              responses:
                "204":
                  description: Application deregistered
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                    life-cycle-state:
                      description: Life cycle state of the consumed service
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: EXPERIMENTAL
                        enum:
                        - EXPERIMENTAL
                        - OPERATIONAL
                        - DEPRECATED
                        - OBSOLETE
                        - UNKNOWN
                        - NOT_YET_DEFINED
                "400":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "401":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "403":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "404":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "500":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                default:
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
        PromptingNewReleaseForUpdatingServerCausesRequestForBroadcastingInfoAboutBackwardCompatibleUpdateOfOperation:
          url:
            description: |
              'This callback belongs to the sequence of actions that have to be done during the bequeath-your-data-and-die process, despite the forwarding gets neither managed nor directly initiated by the /v1/bequeath-your-data-and-die request. After consuming applications have been redirected to the new release, the new release is triggered (this callback) to request the RegistryOffice for broadcasting information about backward compatible replacements of services.'
            post:
              parameters:
              - name: user
                in: header
                description: User identifier from the system starting the service
                  call
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: User Name
              - name: originator
                in: header
                description: |
                  'Identification for the system consuming the API, as defined in
                   [/core-model-1-4:control-construct/logical-termination-point={uuid}/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
                required: true
                style: simple
                explode: false
                schema:
                  minLength: 3
                  type: string
                  example: Resolver
              - name: x-correlator
                in: header
                description: UUID for the service execution flow that allows to correlate
                  requests and responses
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 550e8400-e29b-11d4-a716-446655440000
              - name: trace-indicator
                in: header
                description: Sequence of request numbers along the flow
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^([0-9]+)(\\.([0-9]+))*$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 1.3.1
              - name: customer-journey
                in: header
                description: Holds information supporting customer’s journey to which
                  the execution applies
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: Unknown value
              requestBody:
                content:
                  application/json:
                    schema:
                      required:
                      - application-name
                      - new-operation-name
                      - old-operation-name
                      - release-number
                      type: object
                      properties:
                        application-name:
                          type: string
                          description: |
                            'Own application name from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-s-000/layer-protocol=0/http-server-interface-1-0:http-server-interface-pac/http-server-interface-capability/application-name]'
                        release-number:
                          type: string
                          description: |
                            'Own release number from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-s-000/layer-protocol=0/http-server-interface-1-0:http-server-interface-pac/http-server-interface-capability/release-number]'
                        old-operation-name:
                          type: string
                          description: |
                            'Name of the deprecated operation from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-op-s-*/layer-protocol=0/operation-server-interface-1-0:operation-server-interface-pac/operation-server-interface-capability/operation-name]'
                        new-operation-name:
                          type: string
                          description: |
                            'Name of the replacing operation from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-op-s-*/layer-protocol=0/operation-server-interface-1-0:operation-server-interface-pac/operation-server-interface-capability/operation-name]'
                      example:
                        application-name: OperationKeyManagement
                        release-number: 2.0.0
                        old-operation-name: /v1/regard-application
                        new-operation-name: /v2/regard-application
                required: true
              responses:
                "204":
                  description: Will broadcast operation update
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                    life-cycle-state:
                      description: Life cycle state of the consumed service
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: EXPERIMENTAL
                        enum:
                        - EXPERIMENTAL
                        - OPERATIONAL
                        - DEPRECATED
                        - OBSOLETE
                        - UNKNOWN
                        - NOT_YET_DEFINED
                "400":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "401":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "403":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "404":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "500":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                default:
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
      security:
      - apiKeyAuth: []
      x-swagger-router-controller: IndividualServices
  /v1/regard-application:
    post:
      tags:
      - IndividualServices
      summary: Adds to the list of applications
      operationId: regardApplication
      parameters:
      - name: user
        in: header
        description: User identifier from the system starting the service call
        required: true
        style: simple
        explode: false
        schema:
          type: string
          example: User Name
      - name: originator
        in: header
        description: |
          'Identification for the system consuming the API, as defined in
           [/core-model-1-4:control-construct/logical-termination-point={uuid}/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
        required: true
        style: simple
        explode: false
        schema:
          minLength: 3
          type: string
          example: Resolver
      - name: x-correlator
        in: header
        description: UUID for the service execution flow that allows to correlate
          requests and responses
        required: true
        style: simple
        explode: false
        schema:
          pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
          type: string
          description: Empty string accepted from external applications.
          example: 550e8400-e29b-11d4-a716-446655440000
      - name: trace-indicator
        in: header
        description: Sequence of request numbers along the flow
        required: true
        style: simple
        explode: false
        schema:
          pattern: "^([0-9]+)(\\.([0-9]+))*$"
          type: string
          description: Empty string accepted from external applications.
          example: 1.3.1
      - name: customer-journey
        in: header
        description: Holds information supporting customer’s journey to which the
          execution applies
        required: true
        style: simple
        explode: false
        schema:
          type: string
          example: Unknown value
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/v1_regardapplication_body'
        required: true
      responses:
        "204":
          description: Application will be able to receive operationKeys
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
            life-cycle-state:
              description: |
                'Life cycle state of the consumed service find in [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-op-s-is-001/layer-protocol=0/operation-server-interface-1-0:operation-server-interface-pac/operation-server-interface-configuration/life-cycle-state]'
              style: simple
              explode: false
              schema:
                type: string
                example: EXPERIMENTAL
                enum:
                - EXPERIMENTAL
                - OPERATIONAL
                - DEPRECATED
                - OBSOLETE
                - UNKNOWN
                - NOT_YET_DEFINED
        "400":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        "401":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        "403":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        "404":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        "500":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        default:
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
      callbacks:
        LinkUpdateNotificationCausesRequestForLinkEndPoints:
          url:
            post:
              parameters:
              - name: user
                in: header
                description: User identifier from the system starting the service
                  call
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: User Name
              - name: originator
                in: header
                description: |
                  'Identification for the system consuming the API, as defined in
                   [/core-model-1-4:control-construct/logical-termination-point={uuid}/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
                required: true
                style: simple
                explode: false
                schema:
                  minLength: 3
                  type: string
                  example: Resolver
              - name: x-correlator
                in: header
                description: UUID for the service execution flow that allows to correlate
                  requests and responses
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 550e8400-e29b-11d4-a716-446655440000
              - name: trace-indicator
                in: header
                description: Sequence of request numbers along the flow
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^([0-9]+)(\\.([0-9]+))*$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 1.3.1
              - name: customer-journey
                in: header
                description: Holds information supporting customer’s journey to which
                  the execution applies
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: Unknown value
              requestBody:
                content:
                  application/json:
                    schema:
                      required:
                      - link-uuid
                      type: object
                      properties:
                        link-uuid:
                          type: string
                          description: |
                            'UUID of Link that holds the searched end points from [$/v1/regard-updated-link.body#link-uuid]'
                      example:
                        link-uuid: alt-2-0-0-op-link-051
                required: true
              responses:
                "200":
                  description: List of operation UUIDs belonging to given link provided
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                    life-cycle-state:
                      description: Life cycle state of the consumed service
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: EXPERIMENTAL
                        enum:
                        - EXPERIMENTAL
                        - OPERATIONAL
                        - DEPRECATED
                        - OBSOLETE
                        - UNKNOWN
                        - NOT_YET_DEFINED
                  content:
                    application/json:
                      schema:
                        required:
                        - link-end-point-list
                        type: object
                        properties:
                          link-end-point-list:
                            type: array
                            items:
                              required:
                              - application-name
                              - ltp-direction
                              - operation-uuid
                              - release-number
                              type: object
                              properties:
                                application-name:
                                  type: string
                                  description: Name of application that holds the
                                    link end point (operationServer or operationClient)
                                release-number:
                                  type: string
                                  description: Release of application that holds the
                                    link end point (operationServer or operationClient)
                                operation-uuid:
                                  type: string
                                  description: UUID of the operation (server or client)
                                    at the link end point
                                ltp-direction:
                                  type: string
                                  description: Identifies OperationServer (SOURCE)
                                    and OperationClient (SINK)
                                  enum:
                                  - core-model-1-4:TERMINATION_DIRECTION_SOURCE
                                  - core-model-1-4:TERMINATION_DIRECTION_SINK
                        example:
                          link-end-point-list:
                          - application-name: RegistryOffice
                            release-number: 2.0.0
                            operation-uuid: ro-2-0-0-op-c-im-alt-1-0-0-003
                            ltp-direction: core-model-1-4:TERMINATION_DIRECTION_SINK
                          - application-name: ApplicationLayerTopology
                            release-number: 1.0.0
                            operation-uuid: alt-1-0-0-op-s-is-001
                            ltp-direction: core-model-1-4:TERMINATION_DIRECTION_SOURCE
                "400":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "401":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "403":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "404":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "500":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                default:
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
        LinkUpdateNotificationCausesOperationKeyUpdates:
          url:
            post:
              parameters:
              - name: user
                in: header
                description: User identifier from the system starting the service
                  call
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: User Name
              - name: originator
                in: header
                description: |
                  'Identification for the system consuming the API, as defined in
                   [/core-model-1-4:control-construct/logical-termination-point={uuid}/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
                required: true
                style: simple
                explode: false
                schema:
                  minLength: 3
                  type: string
                  example: Resolver
              - name: x-correlator
                in: header
                description: UUID for the service execution flow that allows to correlate
                  requests and responses
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 550e8400-e29b-11d4-a716-446655440000
              - name: trace-indicator
                in: header
                description: Sequence of request numbers along the flow
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^([0-9]+)(\\.([0-9]+))*$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 1.3.1
              - name: customer-journey
                in: header
                description: Holds information supporting customer’s journey to which
                  the execution applies
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: Unknown value
              requestBody:
                content:
                  application/json:
                    schema:
                      required:
                      - new-operation-key
                      - operation-uuid
                      type: object
                      properties:
                        operation-uuid:
                          type: string
                          description: |
                            'UUID of LTP that is target of the updating of the operation key from [$ALT://v1/list-end-points-of-link.response#operation-uuid]'
                        new-operation-key:
                          minLength: 5
                          type: string
                          description: |
                            'Future operation key'
                      example:
                        operation-uuid: ro-2-0-0-op-c-im-alt-1-0-0-003
                        new-operation-key: Operation key not yet provided.
                required: true
              responses:
                "204":
                  description: Operation key has been updated
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                    life-cycle-state:
                      description: Life cycle state of the consumed service
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: EXPERIMENTAL
                        enum:
                        - EXPERIMENTAL
                        - OPERATIONAL
                        - DEPRECATED
                        - OBSOLETE
                        - UNKNOWN
                        - NOT_YET_DEFINED
                "400":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "401":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "403":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "404":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "500":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                default:
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
        CyclicOperationCausesRequestForListOfLinks:
          url:
            post:
              parameters:
              - name: user
                in: header
                description: User identifier from the system starting the service
                  call
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: User Name
              - name: originator
                in: header
                description: |
                  'Identification for the system consuming the API, as defined in
                   [/core-model-1-4:control-construct/logical-termination-point={uuid}/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
                required: true
                style: simple
                explode: false
                schema:
                  minLength: 3
                  type: string
                  example: Resolver
              - name: x-correlator
                in: header
                description: UUID for the service execution flow that allows to correlate
                  requests and responses
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 550e8400-e29b-11d4-a716-446655440000
              - name: trace-indicator
                in: header
                description: Sequence of request numbers along the flow
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^([0-9]+)(\\.([0-9]+))*$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 1.3.1
              - name: customer-journey
                in: header
                description: Holds information supporting customer’s journey to which
                  the execution applies
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: Unknown value
              responses:
                "200":
                  description: List of UUIDs of Links provided
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                    life-cycle-state:
                      description: Life cycle state of the consumed service
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: EXPERIMENTAL
                        enum:
                        - EXPERIMENTAL
                        - OPERATIONAL
                        - DEPRECATED
                        - OBSOLETE
                        - UNKNOWN
                        - NOT_YET_DEFINED
                  content:
                    application/json:
                      schema:
                        required:
                        - link-uuid-list
                        type: object
                        properties:
                          link-uuid-list:
                            type: array
                            items:
                              type: string
                              description: UUID of Link
                        example:
                          link-uuid-list:
                          - alt-2-0-0-op-link-051
                          - alt-2-0-0-op-link-145
                "400":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "401":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "403":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "404":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "500":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                default:
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
        CyclicOperationCausesRequestForLinkEndPoints:
          url:
            post:
              parameters:
              - name: user
                in: header
                description: User identifier from the system starting the service
                  call
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: User Name
              - name: originator
                in: header
                description: |
                  'Identification for the system consuming the API, as defined in
                   [/core-model-1-4:control-construct/logical-termination-point={uuid}/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
                required: true
                style: simple
                explode: false
                schema:
                  minLength: 3
                  type: string
                  example: Resolver
              - name: x-correlator
                in: header
                description: UUID for the service execution flow that allows to correlate
                  requests and responses
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 550e8400-e29b-11d4-a716-446655440000
              - name: trace-indicator
                in: header
                description: Sequence of request numbers along the flow
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^([0-9]+)(\\.([0-9]+))*$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 1.3.1
              - name: customer-journey
                in: header
                description: Holds information supporting customer’s journey to which
                  the execution applies
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: Unknown value
              requestBody:
                content:
                  application/json:
                    schema:
                      required:
                      - link-uuid
                      type: object
                      properties:
                        link-uuid:
                          type: string
                          description: |
                            'UUID of Link with searched end points from [$ALT://v1/list-link-uuids.response#link-uuid-list]'
                      example:
                        link-uuid: alt-2-0-0-op-link-051
                required: true
              responses:
                "200":
                  description: List of operation UUIDs belonging to given link provided
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                    life-cycle-state:
                      description: Life cycle state of the consumed service
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: EXPERIMENTAL
                        enum:
                        - EXPERIMENTAL
                        - OPERATIONAL
                        - DEPRECATED
                        - OBSOLETE
                        - UNKNOWN
                        - NOT_YET_DEFINED
                  content:
                    application/json:
                      schema:
                        required:
                        - link-end-point-list
                        type: object
                        properties:
                          link-end-point-list:
                            type: array
                            items:
                              required:
                              - application-name
                              - ltp-direction
                              - operation-uuid
                              - release-number
                              type: object
                              properties:
                                application-name:
                                  type: string
                                  description: Name of application that holds the
                                    link end point (operationServer or operationClient)
                                release-number:
                                  type: string
                                  description: Release of application that holds the
                                    link end point (operationServer or operationClient)
                                operation-uuid:
                                  type: string
                                  description: UUID of the operation (server or client)
                                    at the link end point
                                ltp-direction:
                                  type: string
                                  description: Identifies OperationServer (SOURCE)
                                    and OperationClient (SINK)
                                  enum:
                                  - core-model-1-4:TERMINATION_DIRECTION_SOURCE
                                  - core-model-1-4:TERMINATION_DIRECTION_SINK
                        example:
                          link-end-point-list:
                          - application-name: RegistryOffice
                            release-number: 2.0.0
                            operation-uuid: ro-2-0-0-op-c-im-alt-1-0-0-003
                            ltp-direction: core-model-1-4:TERMINATION_DIRECTION_SINK
                          - application-name: ApplicationLayerTopology
                            release-number: 1.0.0
                            operation-uuid: alt-1-0-0-op-s-is-001
                            ltp-direction: core-model-1-4:TERMINATION_DIRECTION_SOURCE
                "400":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "401":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "403":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "404":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "500":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                default:
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
        CyclicOperationCausesOperationKeyUpdates:
          url:
            post:
              parameters:
              - name: user
                in: header
                description: User identifier from the system starting the service
                  call
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: User Name
              - name: originator
                in: header
                description: |
                  'Identification for the system consuming the API, as defined in
                   [/core-model-1-4:control-construct/logical-termination-point={uuid}/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
                required: true
                style: simple
                explode: false
                schema:
                  minLength: 3
                  type: string
                  example: Resolver
              - name: x-correlator
                in: header
                description: UUID for the service execution flow that allows to correlate
                  requests and responses
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 550e8400-e29b-11d4-a716-446655440000
              - name: trace-indicator
                in: header
                description: Sequence of request numbers along the flow
                required: true
                style: simple
                explode: false
                schema:
                  pattern: "^([0-9]+)(\\.([0-9]+))*$"
                  type: string
                  description: Empty string accepted from external applications.
                  example: 1.3.1
              - name: customer-journey
                in: header
                description: Holds information supporting customer’s journey to which
                  the execution applies
                required: true
                style: simple
                explode: false
                schema:
                  type: string
                  example: Unknown value
              requestBody:
                content:
                  application/json:
                    schema:
                      required:
                      - new-operation-key
                      - operation-uuid
                      type: object
                      properties:
                        operation-uuid:
                          type: string
                          description: |
                            'UUID of LTP that is target of the updating of the operation key from [$ALT://v1/list-end-points-of-link.response#operation-uuid]'
                        new-operation-key:
                          minLength: 5
                          type: string
                          description: |
                            'Future operation key'
                      example:
                        operation-uuid: ro-2-0-0-op-c-im-alt-1-0-0-003
                        new-operation-key: Operation key not yet provided.
                required: true
              responses:
                "204":
                  description: Operation key has been updated
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                    life-cycle-state:
                      description: Life cycle state of the consumed service
                      style: simple
                      explode: false
                      schema:
                        type: string
                        example: EXPERIMENTAL
                        enum:
                        - EXPERIMENTAL
                        - OPERATIONAL
                        - DEPRECATED
                        - OBSOLETE
                        - UNKNOWN
                        - NOT_YET_DEFINED
                "400":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "401":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "403":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "404":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                "500":
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
                default:
                  description: Response in case of errored service requests
                  headers:
                    x-correlator:
                      description: UUID for the service execution flow that allows
                        to correlate requests and responses. Its value must be identical
                        at the response compared with its corresponding request
                      style: simple
                      explode: false
                      schema:
                        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                        type: string
                        example: 550e8400-e29b-11d4-a716-446655440000
                    exec-time:
                      description: "Value written by the service provider, reporting\
                        \ the total elapsed time for the execution, including all\
                        \ the additional processing needed to retrieve the data from\
                        \ the backend service. Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 1100
                    backend-time:
                      description: "Value written by the service provider, reporting\
                        \ the elapsed time for data retrieval from the backend (service\
                        \ invocation, database access…). Expressed in milliseconds"
                      style: simple
                      explode: false
                      schema:
                        type: integer
                        example: 850
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/errorDescription'
      security:
      - apiKeyAuth: []
      x-swagger-router-controller: IndividualServices
  /v1/disregard-application:
    post:
      tags:
      - IndividualServices
      summary: Removes application from list of targets of operationKeys
      operationId: disregardApplication
      parameters:
      - name: user
        in: header
        description: User identifier from the system starting the service call
        required: true
        style: simple
        explode: false
        schema:
          type: string
          example: User Name
      - name: originator
        in: header
        description: |
          'Identification for the system consuming the API, as defined in
           [/core-model-1-4:control-construct/logical-termination-point={uuid}/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
        required: true
        style: simple
        explode: false
        schema:
          minLength: 3
          type: string
          example: Resolver
      - name: x-correlator
        in: header
        description: UUID for the service execution flow that allows to correlate
          requests and responses
        required: true
        style: simple
        explode: false
        schema:
          pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
          type: string
          description: Empty string accepted from external applications.
          example: 550e8400-e29b-11d4-a716-446655440000
      - name: trace-indicator
        in: header
        description: Sequence of request numbers along the flow
        required: true
        style: simple
        explode: false
        schema:
          pattern: "^([0-9]+)(\\.([0-9]+))*$"
          type: string
          description: Empty string accepted from external applications.
          example: 1.3.1
      - name: customer-journey
        in: header
        description: Holds information supporting customer’s journey to which the
          execution applies
        required: true
        style: simple
        explode: false
        schema:
          type: string
          example: Unknown value
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/v1_disregardapplication_body'
        required: true
      responses:
        "204":
          description: Application will no longer receive operationKeys
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
            life-cycle-state:
              description: |
                'Life cycle state of the consumed service find in [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-op-s-is-002/layer-protocol=0/operation-server-interface-1-0:operation-server-interface-pac/operation-server-interface-configuration/life-cycle-state]'
              style: simple
              explode: false
              schema:
                type: string
                example: EXPERIMENTAL
                enum:
                - EXPERIMENTAL
                - OPERATIONAL
                - DEPRECATED
                - OBSOLETE
                - UNKNOWN
                - NOT_YET_DEFINED
        "400":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        "401":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        "403":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        "404":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        "500":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        default:
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
      security:
      - apiKeyAuth: []
      x-swagger-router-controller: IndividualServices
  /v1/list-applications:
    post:
      tags:
      - IndividualServices
      summary: Provides list of applications that are receiving operationKeys
      operationId: listApplications
      parameters:
      - name: user
        in: header
        description: User identifier from the system starting the service call
        required: true
        style: simple
        explode: false
        schema:
          type: string
          example: User Name
      - name: originator
        in: header
        description: |
          'Identification for the system consuming the API, as defined in
           [/core-model-1-4:control-construct/logical-termination-point={uuid}/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
        required: true
        style: simple
        explode: false
        schema:
          minLength: 3
          type: string
          example: Resolver
      - name: x-correlator
        in: header
        description: UUID for the service execution flow that allows to correlate
          requests and responses
        required: true
        style: simple
        explode: false
        schema:
          pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
          type: string
          description: Empty string accepted from external applications.
          example: 550e8400-e29b-11d4-a716-446655440000
      - name: trace-indicator
        in: header
        description: Sequence of request numbers along the flow
        required: true
        style: simple
        explode: false
        schema:
          pattern: "^([0-9]+)(\\.([0-9]+))*$"
          type: string
          description: Empty string accepted from external applications.
          example: 1.3.1
      - name: customer-journey
        in: header
        description: Holds information supporting customer’s journey to which the
          execution applies
        required: true
        style: simple
        explode: false
        schema:
          type: string
          example: Unknown value
      responses:
        "200":
          description: List of applications provided
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
            life-cycle-state:
              description: |
                'Life cycle state of the consumed service find in [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-op-s-is-003/layer-protocol=0/operation-server-interface-1-0:operation-server-interface-pac/operation-server-interface-configuration/life-cycle-state]'
              style: simple
              explode: false
              schema:
                type: string
                example: EXPERIMENTAL
                enum:
                - EXPERIMENTAL
                - OPERATIONAL
                - DEPRECATED
                - OBSOLETE
                - UNKNOWN
                - NOT_YET_DEFINED
          content:
            application/json:
              schema:
                uniqueItems: true
                type: array
                items:
                  $ref: '#/components/schemas/inline_response_200'
                x-content-type: application/json
        "400":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        "401":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        "403":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        "404":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        "500":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        default:
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
      security:
      - apiKeyAuth: []
      x-swagger-router-controller: IndividualServices
  /v1/regard-updated-link:
    post:
      tags:
      - IndividualServices
      summary: Initiates OperationKey update
      operationId: regardUpdatedLink
      parameters:
      - name: user
        in: header
        description: User identifier from the system starting the service call
        required: true
        style: simple
        explode: false
        schema:
          type: string
          example: User Name
      - name: originator
        in: header
        description: |
          'Identification for the system consuming the API, as defined in
           [/core-model-1-4:control-construct/logical-termination-point={uuid}/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
        required: true
        style: simple
        explode: false
        schema:
          minLength: 3
          type: string
          example: Resolver
      - name: x-correlator
        in: header
        description: UUID for the service execution flow that allows to correlate
          requests and responses
        required: true
        style: simple
        explode: false
        schema:
          pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
          type: string
          description: Empty string accepted from external applications.
          example: 550e8400-e29b-11d4-a716-446655440000
      - name: trace-indicator
        in: header
        description: Sequence of request numbers along the flow
        required: true
        style: simple
        explode: false
        schema:
          pattern: "^([0-9]+)(\\.([0-9]+))*$"
          type: string
          description: Empty string accepted from external applications.
          example: 1.3.1
      - name: customer-journey
        in: header
        description: Holds information supporting customer’s journey to which the
          execution applies
        required: true
        style: simple
        explode: false
        schema:
          type: string
          example: Unknown value
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/v1_regardupdatedlink_body'
        required: true
      responses:
        "204":
          description: OperationKeys will be synchronized on updated Link
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
            life-cycle-state:
              description: |
                'Life cycle state of the consumed service find in [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-op-s-is-004/layer-protocol=0/operation-server-interface-1-0:operation-server-interface-pac/operation-server-interface-configuration/life-cycle-state]'
              style: simple
              explode: false
              schema:
                type: string
                example: EXPERIMENTAL
                enum:
                - EXPERIMENTAL
                - OPERATIONAL
                - DEPRECATED
                - OBSOLETE
                - UNKNOWN
                - NOT_YET_DEFINED
        "400":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        "401":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        "403":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        "404":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        "500":
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
        default:
          description: Response in case of errored service requests
          headers:
            x-correlator:
              description: UUID for the service execution flow that allows to correlate
                requests and responses. Its value must be identical at the response
                compared with its corresponding request
              style: simple
              explode: false
              schema:
                pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
                type: string
                example: 550e8400-e29b-11d4-a716-446655440000
            exec-time:
              description: "Value written by the service provider, reporting the total\
                \ elapsed time for the execution, including all the additional processing\
                \ needed to retrieve the data from the backend service. Expressed\
                \ in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 1100
            backend-time:
              description: "Value written by the service provider, reporting the elapsed\
                \ time for data retrieval from the backend (service invocation, database\
                \ access…). Expressed in milliseconds"
              style: simple
              explode: false
              schema:
                type: integer
                example: 850
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorDescription'
      security:
      - apiKeyAuth: []
      x-swagger-router-controller: IndividualServices
components:
  schemas:
    errorDescription:
      required:
      - code
      - message
      type: object
      properties:
        code:
          type: integer
          format: int32
        message:
          type: string
    genericRepresentation:
      required:
      - consequent-action-list
      - response-value-list
      type: object
      properties:
        response-value-list:
          type: array
          items:
            $ref: '#/components/schemas/genericRepresentation_responsevaluelist'
        consequent-action-list:
          type: array
          items:
            $ref: '#/components/schemas/genericRepresentation_consequentactionlist'
    logicalTerminationPoint:
      required:
      - client-ltp
      - layer-protocol
      - ltp-direction
      - server-ltp
      - uuid
      type: object
      properties:
        uuid:
          type: string
        ltp-direction:
          type: string
        client-ltp:
          uniqueItems: true
          type: array
          items:
            type: string
        server-ltp:
          uniqueItems: true
          type: array
          items:
            type: string
        layer-protocol:
          maxItems: 1
          minItems: 1
          type: array
          items:
            oneOf:
            - required:
              - layer-protocol-name
              - local-id
              - operation-server-interface-1-0:operation-server-interface-pac
              type: object
              properties:
                local-id:
                  type: string
                layer-protocol-name:
                  type: string
                operation-server-interface-1-0:operation-server-interface-pac:
                  required:
                  - operation-server-interface-capability
                  - operation-server-interface-configuration
                  type: object
                  properties:
                    operation-server-interface-capability:
                      required:
                      - operation-name
                      type: object
                      properties:
                        operation-name:
                          type: string
                    operation-server-interface-configuration:
                      required:
                      - life-cycle-state
                      type: object
                      properties:
                        life-cycle-state:
                          type: string
              description: operation server
            - required:
              - http-server-interface-1-0:http-server-interface-pac
              - layer-protocol-name
              - local-id
              type: object
              properties:
                local-id:
                  type: string
                layer-protocol-name:
                  type: string
                http-server-interface-1-0:http-server-interface-pac:
                  required:
                  - http-server-interface-capability
                  type: object
                  properties:
                    http-server-interface-capability:
                      required:
                      - application-name
                      - data-update-period
                      - release-number
                      type: object
                      properties:
                        application-name:
                          type: string
                        release-number:
                          type: string
                        data-update-period:
                          type: string
              description: http server
            - required:
              - layer-protocol-name
              - local-id
              - tcp-server-interface-1-0:tcp-server-interface-pac
              type: object
              properties:
                local-id:
                  type: string
                layer-protocol-name:
                  type: string
                tcp-server-interface-1-0:tcp-server-interface-pac:
                  required:
                  - tcp-server-interface-configuration
                  type: object
                  properties:
                    tcp-server-interface-configuration:
                      required:
                      - description
                      - local-address
                      - local-port
                      - local-protocol
                      type: object
                      properties:
                        description:
                          type: string
                        local-protocol:
                          type: string
                        local-address:
                          type: object
                          properties:
                            ipv-4-address:
                              type: string
                            domain-name:
                              type: string
                        local-port:
                          type: integer
              description: tcp server
            - required:
              - layer-protocol-name
              - local-id
              - operation-client-interface-1-0:operation-client-interface-pac
              type: object
              properties:
                local-id:
                  type: string
                layer-protocol-name:
                  type: string
                operation-client-interface-1-0:operation-client-interface-pac:
                  required:
                  - operation-client-interface-configuration
                  - operation-client-interface-status
                  type: object
                  properties:
                    operation-client-interface-configuration:
                      required:
                      - operation-name
                      type: object
                      properties:
                        operation-name:
                          type: string
                    operation-client-interface-status:
                      required:
                      - life-cycle-state
                      - operational-state
                      type: object
                      properties:
                        operational-state:
                          type: string
                        life-cycle-state:
                          type: string
              description: operation client
            - required:
              - elasticsearch-client-interface-1-0:elasticsearch-client-interface-pac
              - layer-protocol-name
              - local-id
              type: object
              properties:
                local-id:
                  type: string
                layer-protocol-name:
                  type: string
                elasticsearch-client-interface-1-0:elasticsearch-client-interface-pac:
                  required:
                  - elasticsearch-client-interface-configuration
                  - elasticsearch-client-interface-status
                  type: object
                  properties:
                    elasticsearch-client-interface-configuration:
                      required:
                      - index-alias
                      type: object
                      properties:
                        index-alias:
                          type: string
                    elasticsearch-client-interface-status:
                      required:
                      - life-cycle-state
                      - operational-state
                      type: object
                      properties:
                        operational-state:
                          type: string
                        life-cycle-state:
                          type: string
              description: elasticsearch client
            - required:
              - http-client-interface-1-0:http-client-interface-pac
              - layer-protocol-name
              - local-id
              type: object
              properties:
                local-id:
                  type: string
                layer-protocol-name:
                  type: string
                http-client-interface-1-0:http-client-interface-pac:
                  required:
                  - http-client-interface-configuration
                  type: object
                  properties:
                    http-client-interface-configuration:
                      required:
                      - application-name
                      - release-number
                      type: object
                      properties:
                        application-name:
                          type: string
                        release-number:
                          type: string
              description: http client
            - required:
              - layer-protocol-name
              - local-id
              - tcp-client-interface-1-0:tcp-client-interface-pac
              type: object
              properties:
                local-id:
                  type: string
                layer-protocol-name:
                  type: string
                tcp-client-interface-1-0:tcp-client-interface-pac:
                  required:
                  - tcp-client-interface-configuration
                  type: object
                  properties:
                    tcp-client-interface-configuration:
                      required:
                      - remote-address
                      - remote-port
                      - remote-protocol
                      type: object
                      properties:
                        remote-protocol:
                          type: string
                        remote-address:
                          type: object
                          properties:
                            ip-address:
                              type: object
                              properties:
                                ipv-4-address:
                                  type: string
                            domain-name:
                              type: string
                        remote-port:
                          type: integer
              description: tcp client
    forwardingDomain:
      required:
      - forwarding-construct
      - uuid
      type: object
      properties:
        uuid:
          type: string
        forwarding-construct:
          type: array
          items:
            $ref: '#/components/schemas/forwardingDomain_forwardingconstruct'
    v1bequeathyourdataanddie_newapplicationaddress_ipaddress:
      minProperties: 1
      type: object
      properties:
        ipv-4-address:
          pattern: "^((25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.){3}(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$"
          type: string
          description: |
            'IP address of application that shall be target of the handover process update [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-nr-2-0-0-000/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-address/ip-address/ipv-4-address]'
      additionalProperties: false
    v1bequeathyourdataanddie_newapplicationaddress:
      maxProperties: 1
      minProperties: 1
      type: object
      properties:
        ip-address:
          $ref: '#/components/schemas/v1bequeathyourdataanddie_newapplicationaddress_ipaddress'
        domain-name:
          pattern: "^([a-z0-9]+(-[a-z0-9]+)*\\.)+[a-z]{2,}$"
          type: string
          description: |
            'Domain name of application that shall be target of the handover process update [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-nr-2-0-0-000/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-address/domain-name]'
      additionalProperties: false
    v1_bequeathyourdataanddie_body:
      required:
      - new-application-address
      - new-application-name
      - new-application-port
      - new-application-protocol
      - new-application-release
      type: object
      properties:
        new-application-name:
          type: string
          description: |
            'Name of application that shall be target of the handover process update [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-c-nr-2-0-0-000/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
        new-application-release:
          pattern: "^([0-9]{1,2})\\.([0-9]{1,2})\\.([0-9]{1,2})$"
          type: string
          description: |
            'Release of application that shall be target of the handover process update [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-c-nr-2-0-0-000/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/release-number]'
        new-application-protocol:
          type: string
          description: |
            'Protocol to be used for addressing the application that shall be target of the handover process update [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-nr-2-0-0-000/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-protocol]'
          enum:
          - HTTP
          - HTTPS
        new-application-address:
          $ref: '#/components/schemas/v1bequeathyourdataanddie_newapplicationaddress'
        new-application-port:
          maximum: 65535
          minimum: 0
          type: integer
          description: |
            'Port of application that shall be target of the handover process update [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-nr-2-0-0-000/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-port]'
      example:
        new-application-name: NewApplicationName
        new-application-release: 2.0.0
        new-application-protocol: HTTP
        new-application-address:
          ip-address:
            ipv-4-address: 1.1.3.8
        new-application-port: 3008
    v1regardapplication_address_ipaddress:
      minProperties: 1
      type: object
      properties:
        ipv-4-address:
          pattern: "^((25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.){3}(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$"
          type: string
          description: |
            'IP address of application that shall be part of the operationKey management update or create [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-*/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-address/ip-address/ipv-4-address]'
      additionalProperties: false
    v1regardapplication_address:
      maxProperties: 1
      minProperties: 1
      type: object
      properties:
        ip-address:
          $ref: '#/components/schemas/v1regardapplication_address_ipaddress'
        domain-name:
          pattern: "^([a-z0-9]+(-[a-z0-9]+)*\\.)+[a-z]{2,}$"
          type: string
          description: |
            'Domain name of application that shall be part of the operationKey management update or create [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-*/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-address/domain-name]'
      additionalProperties: false
    v1_regardapplication_body:
      required:
      - address
      - application-name
      - port
      - protocol
      - release-number
      type: object
      properties:
        application-name:
          pattern: "([A-Z][a-z0-9]+){2,}"
          minLength: 3
          type: string
          description: |
            'Name of application that shall be part of the operationKey management find or create [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-c-*/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
        release-number:
          pattern: "^([0-9]{1,2})\\.([0-9]{1,2})\\.([0-9]{1,2})$"
          type: string
          description: |
            'Release of application that shall be part of the operationKey management find or create [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-c-*/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/release-number]'
        protocol:
          type: string
          description: |
            'Protocol to be used for operationKey management update or create [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-*/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-protocol]'
          enum:
          - HTTP
          - HTTPS
        address:
          $ref: '#/components/schemas/v1regardapplication_address'
        port:
          maximum: 65535
          minimum: 0
          type: integer
          description: |
            'TCP port of application that shall be part of the operationKey management update or create [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-*/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-port]'
      example:
        application-name: RegistryOffice
        release-number: 2.0.0
        protocol: HTTP
        address:
          ip-address:
            ipv-4-address: 1.1.3.8
        port: 3008
    v1_disregardapplication_body:
      required:
      - application-name
      - release-number
      type: object
      properties:
        application-name:
          type: string
          description: |
            'Name of application that shall no longer be part of the operationKey management find in [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-c-*/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
        release-number:
          pattern: "^([0-9]{1,2})\\.([0-9]{1,2})\\.([0-9]{1,2})$"
          type: string
          description: |
            'Release of application that shall no longer be part of the operationKey management find in [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-c-*/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/release-number]'
      example:
        application-name: CurrentController
        release-number: 0.0.6
    v1listapplications_address_ipaddress:
      type: object
      properties:
        ipv-4-address:
          type: string
          description: |
            'from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-*/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-address/ip-address/ipv-4-address]'
    v1listapplications_address:
      type: object
      properties:
        ip-address:
          $ref: '#/components/schemas/v1listapplications_address_ipaddress'
        domain-name:
          type: string
          description: |
            'from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-*/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-address/domain-name]'
    inline_response_200:
      required:
      - address
      - application-name
      - port
      - protocol
      - release-number
      type: object
      properties:
        application-name:
          type: string
          description: |
            'from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-c-*/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
        release-number:
          type: string
          description: |
            'from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-http-c-*/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/release-number]'
        protocol:
          type: string
          description: |
            'from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-*/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-protocol]'
        address:
          $ref: '#/components/schemas/v1listapplications_address'
        port:
          type: integer
          description: |
            'from [/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-c-*/layer-protocol=0/tcp-client-interface-1-0:tcp-client-interface-pac/tcp-client-interface-configuration/remote-port]'
      example:
      - application-name: RegistryOffice
        release-number: 2.0.0
        protocol: HTTP
        address:
          ip-address:
            ipv-4-address: 1.1.3.8
        port: 3008
      - application-name: TypeApprovalRegister
        release-number: 1.0.0
        protocol: HTTP
        address:
          ip-address:
            ipv-4-address: 1.1.3.2
        port: 3002
    v1_regardupdatedlink_body:
      required:
      - link-uuid
      type: object
      properties:
        link-uuid:
          pattern: "^([a-z]{2,6})-([0-9]{1,2})-([0-9]{1,2})-([0-9]{1,2})-op-link-([0-9]{3})$"
          type: string
      example:
        link-uuid: alt-2-0-0-op-link-051
    genericRepresentation_responsevaluelist:
      required:
      - datatype
      - field-name
      - value
      type: object
      properties:
        field-name:
          type: string
          description: |
            'Field name from [/core-model-1-4:control-construct/profile-collection/profile=okm-2-0-0-response-p-*/response-profile-1-0:response-profile-pac/response-profile-capability/field-name]'
        value:
          type: string
          description: |
            'Field value from [/core-model-1-4:control-construct/profile-collection/profile=okm-2-0-0-response-p-*/response-profile-1-0:response-profile-pac/response-profile-configuration/value]'
        datatype:
          type: string
          description: |
            'Field datatype from [/core-model-1-4:control-construct/profile-collection/profile=okm-2-0-0-response-p-*/response-profile-1-0:response-profile-pac/response-profile-capability/datatype]'
    genericRepresentation_inputvaluelist:
      required:
      - field-name
      type: object
      properties:
        field-name:
          type: string
          description: |
            'Name of an input value required for executing the Request from [/core-model-1-4:control-construct/profile-collection/profile=okm-2-0-0-action-p-*/action-profile-1-0:action-profile-pac/action-profile-capability/input-value-list=*/name]'
        unit:
          type: string
          description: |
            'Unit of an input value required for executing the Request from [/core-model-1-4:control-construct/profile-collection/profile=okm-2-0-0-action-p-*/action-profile-1-0:action-profile-pac/action-profile-capability/input-value-list=*/unit]'
    genericRepresentation_consequentactionlist:
      required:
      - display-in-new-browser-window
      - label
      - request
      type: object
      properties:
        label:
          type: string
          description: |
            'Label that shall be presented on the button from [/core-model-1-4:control-construct/profile-collection/profile=okm-2-0-0-action-p-*/action-profile-1-0:action-profile-pac/action-profile-capability/label]'
        request:
          type: string
          description: |
            'Request that shall be called, when button gets pressed from [{/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-s-*/layer-protocol=0/tcp-server-interface-1-0:tcp-server-interface-pac/tcp-server-interface-configuration/local-protocol}] :// [{/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-s-*/layer-protocol=0/tcp-server-interface-1-0:tcp-server-interface-pac/tcp-server-interface-configuration/local-address/ipv-4-address} or {/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-s-*/layer-protocol=0/tcp-server-interface-1-0:tcp-server-interface-pac/tcp-server-interface-configuration/local-address/domain-name}] : [{/core-model-1-4:control-construct/logical-termination-point=okm-2-0-0-tcp-s-*/layer-protocol=0/tcp-server-interface-1-0:tcp-server-interface-pac/tcp-server-interface-configuration/local-port}] [{/core-model-1-4:control-construct/profile-collection/profile=okm-2-0-0-action-p-*/action-profile-1-0:action-profile-pac/action-profile-configuration/consequent-operation-reference}]'
        input-value-list:
          type: array
          items:
            $ref: '#/components/schemas/genericRepresentation_inputvaluelist'
        display-in-new-browser-window:
          type: boolean
          description: |
            'True in case Request shall be represented in a new browser window from [/core-model-1-4:control-construct/profile-collection/profile=okm-2-0-0-action-p-*/action-profile-1-0:action-profile-pac/action-profile-capability/display-in-new-browser-window]'
    forwardingDomain_name:
      required:
      - value
      - value-name
      type: object
      properties:
        value-name:
          type: string
        value:
          type: string
    forwardingDomain_fcport:
      required:
      - local-id
      - logical-termination-point
      - port-direction
      type: object
      properties:
        local-id:
          type: string
        port-direction:
          type: string
        logical-termination-point:
          type: string
    forwardingDomain_forwardingconstruct:
      required:
      - fc-port
      - name
      - uuid
      type: object
      properties:
        uuid:
          type: string
        name:
          type: array
          items:
            $ref: '#/components/schemas/forwardingDomain_name'
        fc-port:
          type: array
          items:
            $ref: '#/components/schemas/forwardingDomain_fcport'
  responses:
    responseForErroredServiceRequests:
      description: Response in case of errored service requests
      headers:
        x-correlator:
          description: UUID for the service execution flow that allows to correlate
            requests and responses. Its value must be identical at the response compared
            with its corresponding request
          style: simple
          explode: false
          schema:
            pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
            type: string
            example: 550e8400-e29b-11d4-a716-446655440000
        exec-time:
          description: "Value written by the service provider, reporting the total\
            \ elapsed time for the execution, including all the additional processing\
            \ needed to retrieve the data from the backend service. Expressed in milliseconds"
          style: simple
          explode: false
          schema:
            type: integer
            example: 1100
        backend-time:
          description: "Value written by the service provider, reporting the elapsed\
            \ time for data retrieval from the backend (service invocation, database\
            \ access…). Expressed in milliseconds"
          style: simple
          explode: false
          schema:
            type: integer
            example: 850
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/errorDescription'
    responseForErroredOamRequests:
      description: Response in case of errored OaM requests
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/errorDescription'
  parameters:
    user:
      name: user
      in: header
      description: User identifier from the system starting the service call
      required: true
      style: simple
      explode: false
      schema:
        type: string
        example: User Name
    originator:
      name: originator
      in: header
      description: |
        'Identification for the system consuming the API, as defined in
         [/core-model-1-4:control-construct/logical-termination-point={uuid}/layer-protocol=0/http-client-interface-1-0:http-client-interface-pac/http-client-interface-configuration/application-name]'
      required: true
      style: simple
      explode: false
      schema:
        minLength: 3
        type: string
        example: Resolver
    x-correlator:
      name: x-correlator
      in: header
      description: UUID for the service execution flow that allows to correlate requests
        and responses
      required: true
      style: simple
      explode: false
      schema:
        pattern: "^[0-9A-Fa-f]{8}(?:-[0-9A-Fa-f]{4}){3}-[0-9A-Fa-f]{12}$"
        type: string
        description: Empty string accepted from external applications.
        example: 550e8400-e29b-11d4-a716-446655440000
    trace-indicator:
      name: trace-indicator
      in: header
      description: Sequence of request numbers along the flow
      required: true
      style: simple
      explode: false
      schema:
        pattern: "^([0-9]+)(\\.([0-9]+))*$"
        type: string
        description: Empty string accepted from external applications.
        example: 1.3.1
    customer-journey:
      name: customer-journey
      in: header
      description: Holds information supporting customer’s journey to which the execution
        applies
      required: true
      style: simple
      explode: false
      schema:
        type: string
        example: Unknown value
  securitySchemes:
    apiKeyAuth:
      type: apiKey
      name: operation-key
      in: header
    basicAuth:
      type: http
      scheme: basic
